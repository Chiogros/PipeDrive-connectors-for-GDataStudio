function getAuthType() {
  return PipeDriveCore.getAuthType();
}

function isAdminUser() {
  return PipeDriveCore.isAdminUser();
}

function checkForValidCredentials(domain, endpoint, key) {
  return PipeDriveCore.checkForValidCredentials(domain, endpoint, key);
}
